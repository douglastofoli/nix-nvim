{...}: {
  nvim = {
    # completion = {
    #   enable = true;
    #   buffer.enable = true;
    #   cmdline.enable = true;
    #   lsp = {
    #     enable = true;
    #     lspkind.enable = true;
    #   };
    # };

    ui = {
      statusline = {
        enable = true;
        theme = "dracula";
      };
      whichkey.enable = true;
    };
  };
}
